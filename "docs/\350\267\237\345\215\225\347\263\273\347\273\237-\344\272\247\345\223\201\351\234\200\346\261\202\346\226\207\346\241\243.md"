# TS 体育 · 跟单系统 — 产品需求文档

| 项 | 内容 |
|---|---|
| 产品 | TS 体育（AI 赛事推荐） |
| 模块 | 跟单（方案跟投） |
| 版本 | V1.1（用户晒单 + 免审/改标题才审 + 跟单提醒 + 全中额外奖励） |
| 日期 | 2026-08-28 |
| 关联页面 | 首页新增模块；跟单广场；方案详情（含发布人数据）；作者主页；晒单用户页；确认跟单；我的跟单；我的投注；消息；运营后台 |

---

## 1. 背景与目标

### 1.1 背景

当前首页以**单场推荐**为主：焦点赛、Recommend 横滑、Hot 大卡、全部赛事列表。用户每次只对一场球的 1 / X / 2 做决策，缺少「把多场推荐打包成一个可执行方案」的入口。

跟单要解决的是：

- 用户看懂推荐，但不知道怎么组合、下多少、跟谁的风格
- 单场点击路径长，串关需要自己拼
- AI 推荐能力分散在算法里，没有人格化、可 comparе、可追捧的载体

### 1.2 什么是跟单

**跟单 = 用户一键使用某作者发布的整套方案去下注。**

- **方案**：一连串推荐注项（至少 1 场，通常 2–4 场）组成的投注包，展示**总赔率**（各注项赔率连乘）
- **作者**：平台编造并运营的 AI 策略号，不是真实用户；对 C 端以「作者」呈现，并明示 AI 属性（V1 主货盘）
- **晒单用户**（V1.1）：真实用户把自己未开赛的投注单晒到广场；未改系统标题则免审立刻可见，改了标题须过审。展示真实昵称 +「用户晒单」，**不升格为可运营作者**
- **命中率（两条口径，互不写入）**
  - AI 作者：该作者最近 **X** 个已发布已结算**方案**中，全中方案数 / X
  - 用户晒单：该用户最近 **N** 张已结算**投注单**中，猜中数 / N（与分享门槛同口径）
- **跟单动作**：用户输入本金，系统按方案全部注项生成一张串关注单（或等价的方案注单）；跟单逻辑与货盘来源无关

### 1.3 目标

| 目标 | 说明 |
|---|---|
| 首页可发现 | 首屏向下 1–2 屏内看到跟单模块，3 秒内读懂「谁 / 什么方案 / 总赔率 / 命中率」 |
| 一键可跟 | 从卡片到确认跟单 ≤ 2 步，不要求用户自己选盘口 |
| 作者可运营 | 后台能编造作者、绑定不同 AI 策略、控制露出与人设 |
| 数据可解释 | 命中率有明确口径；策略标签对用户可读（如「仅泊松模型」） |
| 风格不分裂 | 跟单模块视觉融入现有深色体育首页，不另起一套品牌 |

### 1.4 成功指标（建议）

| 指标 | 口径 |
|---|---|
| 模块曝光 CTR | 首页跟单卡点击 / 模块曝光 |
| 跟单转化率 | 确认跟单成功 / 方案详情 UV |
| 人均跟单金额 | 跟单成功 GMV / 跟单人数 |
| 作者分发均衡度 | Top1 作者跟单量占比（过高说明货盘单一） |
| 方案完赛结算率 | 已开赛方案按时结算占比 |
| 命中率展示可信投诉 | 用户因「展示命中率与战绩不符」的客诉件数 |

---

## 2. 范围

### 2.1 本期包含（V1）

- 首页「跟单精选」模块
- 跟单广场（更多方案、筛选）
- 方案详情、作者主页
- 确认跟单半层 + 我的跟单
- 运营后台：作者、策略、方案、跟单订单、命中率窗口
- 方案由绑定策略**生成候选**，运营审核后发布（可配置自动发布）
- 作者人设、策略说明、近 X 场命中率

### 2.1b V1.1 增量（用户晒单）

- 用户把自己的未开赛注单「分享到广场」
- 分享门槛（近 N 场已结算注单命中率，可配置）
- **默认标题由系统生成，未改标题则免审、立刻上广场**；只有用户改了标题才进待审核
- 广场可区分「AI 策略号」与「用户晒单」；详情页不出现 AI 人设
- 发布人站内消息：有人跟单（可合并）、截止、结算、奖励到账、改标题驳回
- 方案全中后，平台另发晒单人「跟单总本金 × 奖励比例」（默认 2%，可配）
- 发布人详情突出：跟单人数、总下注额、预期/已发额外奖励
- 配置页增加晒单窗口 N、命中阈值 %、晒单奖励比例 %
- **不**把晒单用户做成可运营作者，**不**把用户注单或奖励写入 AI 作者战绩

### 2.2 本期不包含

- 把真实用户做成可运营「作者」档案、绑定 AI 策略、由运营改人设（晒单 ≠ 达人入驻）
- 采集其他平台的单再发
- 用户互跟、私信、打赏
- 方案内部分注项跟投（只整单跟）
- 从跟单用户本金抽成、达人佣金分润（AI 作者是虚拟号）。平台给晒单人的全中额外奖励见 §15.6，不是抽成
- 跨彩种（篮球等）跟单（信息架构预留，V1 / V1.1 只足球）
- 方案评论区

---

## 3. 角色与原则

### 3.1 角色

| 角色 | 是谁 | 做什么 |
|---|---|---|
| 终端用户 | App 投注用户 | 浏览方案、看作者战绩、一键跟单；达门槛时可晒自己的未开赛注单 |
| 晒单用户 | 达门槛的真实用户 | 分享注单快照；收跟单提醒与全中额外奖励；不获得运营向作者档案，不能绑 AI 策略 |
| 运营 | 内部运营 | 编造作者、配置策略、审核**改了标题**的晒单、配置奖励比例、上下架 |
| 策略/算法 | 内部或模型服务 | 按策略参数产出推荐注项 |
| 风控/财务 | 内部 | 限额、异常跟单、结算核对 |

### 3.2 产品原则

1. **作者是资产，不是 UGC**：所有 **AI 作者**由平台创建，账号体系与真实用户隔离。V1.1 增加用户晒单，是并列货盘，不是把用户升级成作者。
2. **策略可差异化**：不同作者绑定不同算法或参数，对用户用一句话说明「他怎么选」。晒单用户不绑策略。
3. **命中率只认已发生结果，禁止手填**：AI 作者只认已发布已结算方案；用户晒单只认该用户已结算注单。两条线互不写入。
4. **跟单等于整单复制**：用户不能在 V1 改盘口、删腿、改串关结构，只能改本金。晒单被跟时同样整单复制。
5. **身份必须披露**：AI 作者固定「AI 策略号」；用户晒单固定「用户晒单」。禁止互相包装。
6. **禁止外站采集**：不采集其他平台的单再发。晒单只来自本站用户自己的投注单。

---

## 4. 用户场景

| 场景 | 用户诉求 | 期望行为 |
|---|---|---|
| 首页随手跟 | 不想自己挑，想跟一套现成的 | 首页卡上直接点「跟单」，半层输入金额确认 |
| 比较作者 | 想跟命中稳的，或跟风格对口的 | 进作者主页看近 X 场曲线、策略说明、历史方案 |
| 看懂总赔率 | 知道连串后回报多少 | 卡片与详情都强调总赔率；半层实时算「可赢」 |
| 只看某策略 | 只要「亚盘」或「只泊松」 | 广场按策略标签 / 玩法筛选 |
| 追未开赛 | 怕跟到已开始的 | 方案状态：待开赛可跟；任一眼开赛则停止新跟单 |
| 回看自己跟的 | 想知道中没中、赔了多少 | 「我的跟单」按进行中 / 已结算 |
| 晒自己的单 | 觉得这张未开赛注单值得让别人跟 | 「我的投注」点分享到广场；未改标题立刻上广场，改标题过审后上广场 |
| 看自己晒单数据 | 想知道多少人跟、跟了多少、能拿多少奖励 | 已分享卡点「查看数据」进发布人详情 |
| 被人跟了要知道 | 有人跟了自己的晒单 | 消息中心提醒，可点进该方案详情 |

---

## 5. 信息架构与首页位置

### 5.1 页面结构

```
首页
 └─ 跟单精选（新增模块）
     ├─ 方案详情
     │    ├─ 确认跟单（半层）
     │    └─ 作者主页
     └─ 跟单广场（模块右上「更多」）
          ├─ 方案详情 …
          └─ 作者主页
我的 / 记录
 ├─ 我的跟单
 ├─ 消息（通知中心）
 └─ 我的投注（V1.1）
      └─ 分享到广场
           ├─ 未改标题 → 立刻招募中 / 广场可见
           └─ 改了标题 → 待审核 → 过审后广场「用户晒单」
                └─ 已分享 → 查看数据（发布人详情）
```

底部导航建议（可与现网折中）：`首页 | 跟单 | 投注单 | 记录 | 我的`。  
若本期不改 Tab，则「跟单」只从首页模块 + 我的入口进入；「我的投注」放在记录页 Tab。

### 5.2 首页模块位置

放在 **Recommend 横滑下方、Hot 大卡上方**（运营 Banner 若穿插，跟单模块不得被挤出首屏下 1.5 屏）。

理由：Recommend 已经是「系统推荐单场」，跟单是「人格化多场方案」，两者相邻形成「单场 → 方案」的升级路径；又比「全部赛事」列表更适合转化。

### 5.3 首页模块结构

```
跟单精选                              更多 ›
┌─────────────────────────────────────────────┐
│ 头像  作者名     AI 策略号                   │
│       策略短标签   近20场 75%                 │
│ 方案标题 · 3串1                              │
│ 注项 1  主队 vs 客队    主胜    2.10         │
│ 注项 2  …                  让-1    1.85      │
│ 注项 3  …                  大2.5   1.90      │
│ 总赔率 7.38          328 人已跟               │
│ [              一键跟单              ]        │
└─────────────────────────────────────────────┘
（横向最多 3 张精选卡，超出进广场）
```

验收：不点进详情也能回答四件事——谁发的、近 X 场命中、跟哪些盘、总赔多少。

---

## 6. 核心对象与规则

### 6.1 对象关系

```
策略 Strategy  1 ─── N  作者 Author(AI)  1 ─── N  方案 Plan  1 ─── N  跟单订单 Follow
                                              ▲
用户 User  1 ─── N  注单 Bet ──分享审核──► 方案(来源=用户分享)
                      │
                      └── 晒单命中率 = 近 N 张已结算注单（与门槛同口径）
作者近 X 场只认该作者已发布已结算方案；用户注单不得写入作者窗口
```

### 6.2 策略（AI 推荐策略）

运营配置的算法画像，决定「这个作者会推荐什么」。

| 字段 | 说明 | 示例 |
|---|---|---|
| 策略名称 | 对内 | 泊松期望-进球 |
| 用户可见说明 | 一句话，出现在作者卡 | 仅使用泊松 / xG 模型推荐 |
| 算法 / 模型 | **多选**，checkbox 平铺 | 集成模型、泊松 / xG、Elo 实力、亚盘模型、价值偏差、大小球模型 |
| 盘口范围 | 可多选 | 胜平负 / 让球 / 大小球 |
| 联赛池 | 可多选 | 欧冠、英超… |
| 置信度阈值 | 0–1 | 0.62 以下不出单 |
| 单方案腿数 | min–max | 2–4 |
| 单注赔率区间 | | 1.50–2.40 |
| 总赔率区间 | | 3.00–12.00 |
| 发布方式 | | 人工审核 / 自动发布 |
| 状态 | | 启用 / 停用 |

**V1 预置策略（可改名，不可在文档里写死算法细节）：**

| 策略 | 作者侧人设方向 | 用户可见一句话 |
|---|---|---|
| 集成高置信 | 稳健、少出单 | 多模型一致才出单 |
| 泊松 / xG | 数据派 | 仅使用泊松期望模型推荐 |
| 亚盘模型 | 盘口派 | 只推亚洲让球 |
| 价值偏差 | 进攻、高赔 | 找市场定价偏低的选项 |
| 大小球模型 | 进球风格 | 只推大小球 |

同一策略可绑多个作者（人设不同、参数微调）；一个作者 V1 **只绑一个策略**。  
一条策略可勾选多个算法：出单时同时参考被勾选的模型（如「集成高置信」勾集成 + 泊松 + Elo + 价值，要求一致才出单）。

### 6.3 作者（平台编造）

| 字段 | 说明 |
|---|---|
| 显示名 | 对用户，需唯一 |
| 头像 | 运营上传；无图用色块+字 |
| 简介 | ≤ 80 字人设 |
| 标签 | 稳健 / 数据 / 亚盘 / 冷门 等，最多 3 个 |
| 绑定策略 | 必填 |
| 命中率窗口 X | 默认 20，范围 5–50；按作者可覆盖全局默认 |
| 风险等级 | 稳健 / 平衡 / 进取，仅作展示与广场筛选 |
| 状态 | 草稿 / 上架 / 下架 |
| 排序权重 | 首页精选排序因子之一 |
| AI 披露 | 强制，不可关 |

**命名与人设规范：**

- 像「作者」不像「模型代号」：阿尔法狮、泊松君、盘口鹰
- 禁止暗示内幕、必中、庄家、官方情报
- 简介必须能对上绑定策略，禁止「只推亚盘」的作者去发胜平负单

**命中率口径（唯一口径）：**

```
命中率 = 近 X 个「已发布 + 已结算」方案中，结果 = 全中 的数量 / 实际参与计算的场数
```

- **1 场 = 1 个已结算方案**，不是方案里的某一场球
- 走水 / 取消 / 腰斩：该方案不计入窗口（不占 X，往前再取）
- 串关任一腿未中 = 方案未中
- 窗口内实际场数 `n < X`：展示「近 n 场 aa%」或「新锐 · n 场 m 中」，**禁止**用不足样本对外写成「近 X 场」
- `n = 0`：展示「新锐作者」，不展示百分比
- **禁止**运营手填对外命中率
- 回测数据若要展示，必须另字段「回测命中率」且标注「非实盘」，V1 默认不对外

近 X 场明细在作者主页用条形或点阵展示：全中 / 未中 / 走水。

### 6.4 方案

| 字段 | 说明 |
|---|---|
| 标题 | AI / 人工：运营填写。用户晒单：**默认系统生成**（如「英超 2串1」或「赛事名 + 串关」）；用户可展开改标题 |
| 标题是否改过 | 用户晒单：`titleEdited`。未改 = 免审；已改 = 待审 |
| 作者 / 分享人 | AI 方案挂虚拟作者；用户晒单挂真实用户昵称，不挂作者档案 |
| 来源 | 算法生成 / 人工补单 / 用户分享（V1.1） |
| 审核标记 | 用户晒单免审记「免审·未改标题」；改标题记「改标题·待审 / 已通过 / 已驳回」 |
| 注项列表 | 每条：赛事、开赛时间、玩法、选项、当时赔率、赛事 ID |
| 总赔率 | 各注项赔率连乘，展示保留 2 位 |
| 建议本金区间 | 用于半层默认值与校验 |
| 状态 | 见状态机 |
| 截止跟单时间 | 默认 = 最早一腿开赛时间；可提前 N 分钟 |
| 跟单人数 / 金额 | 累计（发布人详情用来算预期奖励） |
| 额外奖励 | 仅用户晒单。未结算：跟单总本金 × 奖励比例（预估）。全中后实发并记 `rewardPaid` |

**注项约束：**

- V1 只足球，只支持：胜平负、亚洲让球、大小球
- 同一方案内赛事不重复（避免同一场多腿争议）
- 每腿必须有可下单的盘口 ID 与赔率快照
- 发布后赔率以**用户跟单当下的实时赔率**重算总赔率；若任一眼不可售或赔率偏离快照超过阈值（建议 10%），拦截并提示「方案已变化，请刷新」

**方案状态机：**

```
候选(算法生成) → 待审核 → 招募中(可跟)
用户分享（未改标题）──────────→ 招募中(可跟)　记「免审·未改标题」
用户分享（改了标题）→ 待审核 → 招募中(可跟)
                         ↓ 驳回（原因用户可见 + 站内通知；该注单不能再提）
                 已截止(有一腿开赛或到截止)
                    ↓
                 结算中 → 已结算(全中 / 未中 / 走水)
任意上架态 可 → 已下架（运营，停止新跟，已跟单仍结算）
```

| 状态 | 用户侧 | 可否新跟单 |
|---|---|---|
| 招募中 | 正常展示 | 是 |
| 已截止 | 详情可看，按钮「已开赛」 | 否 |
| 已结算 | 战绩回看 | 否 |
| 已下架 | 广场不露，已跟用户仍可见自己的单 | 否 |

**结算全中 / 结算未中（方案级，不是单场）：**

方案是一串注项（如 3串1）。赛后整单只有一个结果，用来派奖，并计入作者近 X 场：

| 操作 | 含义 | 跟单结果 | 命中率窗口 |
|---|---|---|---|
| 结算全中 | 方案里**每一腿都打中** | 按总赔率派奖 | 记 1 场「中」 |
| 结算未中 | **至少一腿没打中** | 整单输光本金 | 记 1 场「不中」 |
| 走水（自动/另入口） | 取消、腰斩等无法正常判 | 按现网走水规则退还 | **不计入** X |

正式环境应先接赛果自动判定。后台这两颗按钮是**人工兜底**：赛果未回或要纠错时，把整份方案标成全中或未中，不是把某一场单独结算。

### 6.5 跟单订单

| 字段 | 说明 |
|---|---|
| 用户 | 真实用户 |
| 方案 | 跟投的方案 |
| 本金 | 用户输入 |
| 跟单时总赔率 | 下单快照 |
| 可赢金额 | 本金 × 总赔率（展示规则与现网串关一致） |
| 状态 | 待开赛 / 进行中 / 全中 / 未中 / 走水 / 失败 |

规则：

- 同一用户对同一方案 V1 **可跟 1 次**（降低重复、对账简单；若业务要加注，V1.1 再开）
- 本金受：用户余额、方案建议区间、用户单笔限额、方案剩余可跟额度（如有）
- 下单失败（盘口关闭、余额不足、赔率漂移）必须有明确文案，不生成成功单
- 结算与现网串关结算对齐，跟单不是第二套开奖逻辑

---

## 7. 前端功能需求

### 7.1 首页 · 跟单精选

| 项 | 说明 |
|---|---|
| 数据 | 招募中方案，按 `权重 + 命中率 + 跟单热度 + 开赛临近` 综合，最多 3 条 |
| 横滑 | 卡片宽度约屏宽 86%，露出下一张 |
| 点击卡片非按钮区 | 进方案详情 |
| 点击作者区 | 进作者主页 |
| 点击「一键跟单」 | 已登录则出半层；未登录走现网登录 |
| 「更多」 | 跟单广场 |
| 空态 | 模块隐藏，不占位（避免空货盘） |
| 刷新 | 与首页下拉刷新一起更新 |

### 7.2 跟单广场

- 筛选：来源（全部 / AI 策略号 / 用户晒单）、策略标签、风险等级、总赔率档（3 以下 / 3–8 / 8 以上）、仅看可跟
- 排序：综合（默认）/ 命中率 / 总赔率 / 最多人跟 / 即将截止
- 列表卡信息密度与首页卡一致，可略压缩图；用户晒单用「用户晒单」徽章，不用「AI 策略号」
- 无结果：空态 + 清空筛选

### 7.3 方案详情

固定结构（上到下）：

1. 作者 / 分享人条：
   - AI 方案：头像、名、**AI 策略号**、近 X 场方案命中率、策略一句说明
   - 用户晒单：头像、真实昵称、**用户晒单**、近 N 场注单命中率；**不出现**「AI 策略号」和策略说明
2. 方案头：标题、串关形态（如 3串1）、状态、截止时间倒计时
3. 注项列表：赛事、开赛、玩法、选项、赔率；已开赛注项置灰
4. 总赔率条：大号总赔 + 若跟 100 的示例可赢
5. 跟单数据：已跟人数、已跟金额（金额可按地区开关）
6. **发布人数据（仅当当前登录用户 = 该晒单分享人）**：跟单人数、总下注额、预期/已发额外奖励。未结算标明「若全中预估，未结算」；全中后「已发放 xxx」。别人进同一方案**不展示**此模块
7. 风险提示：AI 方案写「AI 推荐不保证盈利」；用户晒单必须写明「用户分享、非官方/非 AI」，过往命中不代表未来

底栏常驻：「跟单」主按钮；不可跟时按钮禁用并说明原因。  
发布人可从「我的投注」已分享卡、「查看数据」、消息通知一跳进入该详情。

### 7.4 作者主页

- 人设区：头像、名、简介、标签、绑定策略说明
- 战绩区：近 X 场命中率、全中/未中/计入场次、近 X 场点阵
- 进行中方案
- 历史方案（已结算）
- V1 **不提供「关注作者」**（降低社交预期；需要时 V1.1 再加）

### 7.5 确认跟单半层

- 回显：作者、方案名、腿数、**当前实时总赔率**
- 本金输入 + 快捷档（20 / 50 / 100 / 200，可按币种配置）
- 实时「可赢 ≈ 本金 × 总赔率」
- 余额、最小/最大本金
- 主按钮「确认跟单」；成功 toast + 可去「我的跟单」
- 关闭半层不下注

### 7.6 我的跟单

- Tab：进行中 / 已结算
- 卡片：作者或晒单用户、方案名、本金、总赔率、可赢/实际盈亏、状态
- 点进方案详情（只读视角 + 自己的订单信息）

### 7.7 我的投注（V1.1）

- 与「我的跟单」并列，列出用户自己的投注单（单关 / 串关）
- 未开赛或全部未截止的单展示「分享到广场」
- 未登录 / 未达门槛：按钮可见但禁用，并说明原因（还差几场，或「近 N 场需猜中 x 成，当前 w/n」）
- 达门槛：确认半层预览注项、总赔、系统标题、将以真实昵称 +「用户晒单」展示。**默认不展示标题输入框**；可点「修改标题」展开
- 未改标题：确认后 toast「已发布到广场」，状态=招募中，广场立刻可见
- 改了标题：确认后 toast「已提交审核」，状态=待审核
- 同一注单只能分享 1 次
- 已上广场：按钮改为「查看数据」，进入发布人详情
- 审核中：可进详情看审核态；已驳回展示原因（不可再提）
- 已开赛、已结算、已取消：不提供可点分享（或禁用并说明）

### 7.8 消息 / 通知中心

- 入口：顶栏铃铛或「消息」（记录页、首页、广场、详情均可进）
- 只做站内消息，不做推送厂商对接（V1.1）
- 列表按时间倒序；未读角标；点一条进对应方案详情并标已读
- 触发与合并规则见 §15.5

---

## 8. 后台功能需求

后台服务内部运营，不面向 C 端。信息架构：

```
概览 | 作者 | 策略 | 方案 | 跟单订单 | 配置
```

### 8.1 概览

- 今日：新发布方案、跟单人数、跟单 GMV、待审核方案数（算法候选 + **改了标题**的用户晒单）
- 作者排行：跟单量、近窗口命中率（仅 AI 作者）
- 待办：待审核、**待审用户晒单**（只统计改了标题且状态=待审核；免审单不计入）、赔率漂移拦截、结算异常

### 8.2 作者管理

- 列表：名、策略、状态、窗口 X、近 X 命中、进行中方案数、累计跟单
- 新建 / 编辑：§6.3 全部字段；头像上传
- 上架校验：已绑启用中的策略、有头像或默认头、简介非空
- 下架：首页与广场立即不可见；不删除历史方案与订单
- **没有「编辑命中率」**

### 8.3 策略管理

- CRUD §6.2 字段
- 停用策略：已绑作者不能新发方案，已招募方案可继续到截止
- 修改参数不追溯已发布方案

### 8.4 方案管理

- 来源分三种，列表需标明：
  - **按策略生成候选**：系统按作者已绑策略出单
  - **人工补单**：运营点「人工补单」，**作者栏必选、由人工指定**（下拉展示作者名 + 状态 + 绑定策略）。再录入标题与注项；总赔率由各腿赔率连乘，不手填。默认进待审核；作者已上架时可选「保存并发布」直接进入招募中
  - **用户分享**（V1.1）：用户从自己的注单提交；挂真实昵称，不选 AI 作者、不绑策略。列表标明「免审·未改标题」或「改标题·待审/已通过/已驳回」
- 人工补单不改作者命中率口径：只有发布并结算后才计入近 X 场
- 用户晒单**永不计入**任何 AI 作者近 X 场；晒单奖励也不写入作者战绩
- 审核台：仅**改了标题**的晒单需要过审/驳回（驳回须填用户可见原因，并发站内通知）。未改标题的晒单提交即招募中，后台可下架/结算，不必再点通过
- 结算全中：向晒单人发放额外奖励（跟单总本金 × 配置比例），并通知「奖励到账」
- 自动发布：仅当策略配置了自动发布 **且** 通过风控规则（联赛白名单、总赔区间、无重复赛事）
- 强制下架、提前截止
- 结算：优先接现网赛果；异常可人工确认全中/未中/走水

### 8.5 跟单订单

- 筛：时间、作者、方案、用户、状态
- 只读为主；失败单展示失败原因
- 不提供改命中结果（改结果走方案结算）

### 8.6 配置

| 配置 | 默认 | 说明 |
|---|---|---|
| 全局默认 X | 20 | 作者未覆盖时用 |
| X 的允许范围 | 5–50 | |
| 首页精选条数 | 3 | |
| 赔率漂移拦截 | 10% | 相对方案发布快照 |
| 截止提前量 | 开赛前 5 分钟 | |
| 单用户单方案 | 1 次 | |
| 金额对外 | 开 | 可关「已跟金额」只留人数 |
| 晒单窗口 N | 10 | 用户近 N 张已结算注单；与作者窗口 X 分开 |
| 晒单命中阈值 | 70% | 达到才可分享；不足 N 场不能分享 |
| 晒单奖励比例 | 2% | 方案全中后，奖励 = 该方案跟单总本金 × 该比例。未中/走水不发 |

---

## 9. 生成与发布流程

```
1. 运营创建策略（勾选一个或多个算法 / 模型 + 参数 + 用户可见说明）
2. 运营编造作者并绑定策略，上架
3. 定时或手动「按策略生成候选方案」
4. 审核（或自动发布）→ 招募中 → 首页/广场露出
5. 用户跟单 → 现网下单
6. 开赛截止 → 赛后结算 → 滚动更新作者近 X 场命中率
```

生成失败（不够置信、不够腿、赔率不在区间）记入候选失败原因，不硬凑单。  
**宁缺毋滥**：当天无合格方案时，首页模块隐藏。

**V1.1 用户晒单并行流程：**

```
1. 用户在「我的投注」点未开赛注单「分享到广场」
2. 半层默认展示系统标题，不展示标题输入；可展开「修改标题」
3. 提交时服务端再校验：门槛、未开赛、未重复分享、盘口可售；若改了标题则校验违禁词（必中/内幕/稳赚等直接拦）
4. 未改标题：方案状态=招募中，广场立刻可见，后台记「免审·未改标题」
   改了标题：状态=待审核 → 运营过审进广场 / 驳回（原因用户可见 + 通知）
5. 其他人整单跟投（与 AI 方案同一套跟单逻辑）→ 发布人收到消息（短时间多笔可合并）
6. 截止 / 结算全中或未中 → 通知发布人；全中则按「跟单总本金 × 奖励比例」发放额外奖励并通知到账
7. 结算只影响跟单订单与晒单奖励；用户晒单展示命中率仍用该用户近 N 场注单，奖励不写进 AI 作者窗口
```

---

## 10. 接口概要（前后端对齐）

仅列资源，路径可按现网规范调整。

| 方法 | 资源 | 说明 |
|---|---|---|
| GET | `/feed/copy-plans` | 首页精选 |
| GET | `/copy-plans` | 广场列表 + 筛排 |
| GET | `/copy-plans/{id}` | 详情（含实时总赔；若当前用户是发布人则带奖励字段） |
| GET | `/authors/{id}` | 作者 + 战绩点阵 + 方案列表 |
| POST | `/copy-plans/{id}/follow` | 跟单（本金）；成功后给晒单发布人写通知（可合并） |
| GET | `/me/follows` | 我的跟单 |
| GET | `/me/bets` | 我的投注单 + 门槛结果 |
| POST | `/me/bets/{id}/share` | 分享到广场。body：`titleEdited`、可选 `title`。服务端再校验门槛 / 未开赛 / 未重复 / 违禁词 |
| GET | `/me/notifications` | 站内消息列表 |
| POST | `/me/notifications/{id}/read` | 标已读 |
| GET/POST/PUT | `/admin/strategies` | 策略 |
| GET/POST/PUT | `/admin/authors` | 作者 |
| GET/POST | `/admin/plans` | 方案、生成、审核 |
| POST | `/admin/plans/{id}/settle` | 人工结算兜底 |
| GET | `/admin/follows` | 订单 |
| GET/PUT | `/admin/settings` | 全局配置（含晒单 N、命中阈值、晒单奖励比例） |
| POST | `/admin/plans/{id}/review` | 通过 / 驳回（仅改标题晒单；驳回带用户可见原因并通知） |

跟单接口必须服务端再校验：方案可跟、赔率漂移、余额、重复跟、限额。

---

## 11. 埋点

| 事件 | 关键属性 |
|---|---|
| `copy_module_expose` | 位置（首页/广场）、方案 id、作者 id |
| `copy_card_click` | 方案 id、点击热区（卡/作者/跟单） |
| `copy_follow_sheet_show` | 方案 id、展示总赔 |
| `copy_follow_submit` | 方案 id、本金、当时总赔、成功/失败码 |
| `author_profile_view` | 作者 id、来源 |
| `copy_filter_apply` | 广场筛选值 |
| `ugc_share_tap` | 注单 id、是否可点、不可点原因 |
| `ugc_share_submit` | 注单 id、门槛 w/n、是否改标题、免审/待审、成功/失败码 |
| `ugc_review` | 方案 id、通过/驳回、驳回原因码 |
| `ugc_notify_click` | 消息类型、方案 id |
| `ugc_owner_detail` | 方案 id、跟单人数、预估奖励 |

---

## 12. 合规与风险

- 作者是 **AI 策略号**，禁止包装成真人分析师、内部员工、职业赌徒
- 用户晒单必须披露「用户分享」，禁止包装成官方推荐或 AI 策略号
- 过往命中率必须可点开核对（AI：方案列表；晒单：该用户近 N 场注单），禁止只秀百分比
- 文案禁「稳赚、必中、内幕、跟庄」
- 跟单前展示风险提示，与现网投注负责文案对齐；晒单额外提示「过往命中不代表未来」
- 方案与作者数据可审计：谁生成、谁审核、谁改腿、当时赔率快照；晒单另记提交用户与审核意见
- 不把用户单偷偷算进某个 AI 作者战绩
- 若所在地区对「推荐投注 / 跟单」有广告或牌照限制，上线前法务过审；本模块可远程关闭

---

## 13. 验收要点

- 首页 Recommend 与 Hot 之间出现跟单精选，深色风格与现网一致
- 卡片同时露出：作者、策略一句、近 X 场命中、注项、总赔率、跟单按钮
- 跟单半层可改本金、实时可赢、成功后我的跟单看得到
- 任一眼开赛后新用户不能跟
- 后台能建作者、绑不同策略、生成/审核方案；**不能手改命中率**
- 新作者 0 场不展示 xx%；不足 X 场展示「近 n 场」
- 用户可见处：AI 方案均有 AI 策略号披露；用户晒单均有「用户晒单」披露，详情页不出现 AI 策略号
- 未达门槛无法提交分享；同一注单不能重复分享；已开赛/已结算/已取消不能分享
- 未改标题：提交即广场可见，toast「已发布到广场」，后台状态=招募中、标记「免审·未改标题」
- 改了标题：toast「已提交审核」，后台待审；驳回原因用户可见并通知
- 概览「待审用户晒单」只计改了标题的
- 已分享卡可「查看数据」：人数、总额、预期/已发奖励；非发布人看不到「你的奖励」模块
- 有人跟晒单后发布人有消息，点开进详情；短时间多笔可合并
- 配置可改晒单奖励比例；全中按跟单总本金 × 比例发放，未中/走水不发
- 用户晒单结算与奖励不增加任何 AI 作者近 X 场

---

## 14. 分期建议

| 版本 | 内容 |
|---|---|
| **V1** | 虚拟作者 + 策略绑定 + 整单串关跟投 + 首页模块 + 基础后台 |
| **V1.1** | 用户晒单：分享到广场、门槛、**默认免审 / 改标题才审**、站内提醒、全中额外奖励（可配比例）、发布人数据页；**不做**达人入驻成可运营作者 |
| V1.1+ | 关注作者、系统推送、同一方案加注、已跟金额地区开关细化 |
| V2 | 篮球等彩种；部分腿跟投；策略 A/B 与自动发布放量 |
| 不做 | 把真实用户编成可运营 AI 作者、采集外站的单、从跟单本金抽成、评论社区 |

---

## 15. V1.1 用户晒单 / 分享到广场

V1 货盘只有平台编造的 AI 策略号。V1.1 增加**第二条货盘：用户把自己的投注单晒到广场**。两条线并列，不互相冒充。

**为什么是「用户晒单」而不是「用户变成作者」：**  
作者在本产品里是可运营资产——有人设、绑策略、进作者管理、近 X 场按「已发布方案」滚动。若把晒单用户自动做成作者，运营会去改他们的简介、误绑 AI 策略，战绩也会和策略号混在一起，V1「作者是平台编造的 AI」被推翻。晒单只需要：真实昵称、用户徽章、该用户自己的注单命中率、进广场、别人整单跟、跟单提醒与全中额外奖励。不需要作者档案。

### 15.1 谁能分享

| 项 | 规则 |
|---|---|
| 主体 | 已登录的本站用户 |
| 门槛默认 | 近 **10** 张**已结算投注单**命中率 ≥ **70%**（7/10） |
| 1 场口径 | 1 张已结算注单 = 1 场；串关整单算 1 场，不是串关里的每一场球 |
| 计入结果 | 猜中 / 未中；走水、取消、腰斩不占窗口，往前再取 |
| 不足 10 场 | 不能分享，文案说明还差几场 |
| 达到场次但未达阈值 | 不能分享，文案如「近10场需猜中7成，当前 5/10」 |
| 可配置 | 后台：晒单窗口 N、命中阈值 %、晒单奖励比例 %（与作者窗口 X 分开） |
| 未登录 / 未达门槛 | 「分享到广场」**可见但禁用**，并说明原因 |
| 提交时 | 服务端再校验一次门槛，防止提交瞬间掉到阈值下 |

命中率展示与门槛同口径：该用户近 N 场注单，**不要**写进平台 AI 作者的近 X 场。

### 15.2 什么单能分享

| 可分享 | 不可分享 |
|---|---|
| 单关或串关 | 已开赛（任一腿已开赛） |
| 全部注项未开赛、未截止 | 已结算、已取消 |
| 当时的注项快照（赛事、玩法、选项、赔率） | 改过盘口后再装成原单 |
| 总赔率 = 各腿赔率连乘 | 同一注单第二次分享 |

跟单仍是整单复制。广场上别人跟的是这份快照对应的可售盘口；若盘口失效或赔率偏离超过阈值，拦截（与现有跟单一致）。

### 15.3 分享后是谁的「作者」

- 广场与详情以**真实用户昵称**展示，徽章为「用户晒单」（与「AI 策略号」颜色/文案都不同）
- **不**自动创建可运营编辑的作者档案，**不**出现在后台「作者」列表，**不**允许绑 AI 策略
- 晒单用户页（若提供）只读：昵称、用户晒单徽章、近 N 场注单点阵、已过审晒单；无人设策略句、无「AI 策略号」
- 用户晒单的命中率 = 该用户近 N 场注单；AI 作者命中率仍只认该作者已发布已结算方案

### 15.4 标题与审核（默认免审，改标题才审）

**系统标题规则（提交时生成，用户未改则直接用）：**

| 注项 | 标题 |
|---|---|
| 全部同一联赛 | `{联赛} {串关形态}`，如「英超 2串1」「意甲 单关」 |
| 跨联赛 | `{第一场赛事名} {串关形态}`，如「里斯本竞技 vs 布鲁日 3串1」 |

分享半层**默认没有标题输入框**，只展示系统标题。提供「修改标题」展开自定义（建议 ≤ 24 字）。

**分流（拍板，不再「分享必进待审核」）：**

| 用户动作 | 方案状态 | 广场 | 后台 |
|---|---|---|---|
| 未改标题 | 提交即 **招募中** | 立刻可见 | 来源=用户分享，标记「免审·未改标题」 |
| 改了标题（与系统标题不同） | **待审核** | 过审后可见 | 须过审 / 驳回；驳回原因用户可见，并发通知 |
| 改标题后又改回与系统标题完全一致 | 按未改标题处理，免审 | 立刻可见 | 「免审·未改标题」 |

**服务端仍必须自动校验（免审不等于不校验）：**

1. 门槛仍满足（未达门槛用户本来就不能发）
2. 全部注项未开赛、该注单未重复分享
3. 盘口仍可售
4. 若改了标题：含「必中 / 内幕 / 稳赚 / 跟庄 / 必赢 / 内部消息」等**直接拦截**，不进待审、不生成方案

概览待办「待审用户晒单」**只统计改了标题且状态=待审核**的单。免审单不进待办。

驳回后该注单视为已分享过，不能再提。

### 15.5 提醒（站内消息）

只做站内消息 / 通知中心，不做短信或厂商推送。

| 触发 | 文案示例 | 何时发 |
|---|---|---|
| 有人跟了自己的晒单 | 「有人跟了你的英超 2串1」 | 跟单成功后立刻。短时间同一方案多笔未读可合并为「又有 3 人跟了你的英超 2串1」 |
| 方案截止 / 下架 | 「你的英超 2串1 已截止跟单」 | 任一眼开赛到截止，或运营下架 |
| 结算全中 | 「你的英超 2串1 结算全中」 | 方案标为全中时 |
| 结算未中 | 「你的英超 2串1 结算未中」 | 方案标为未中时 |
| 奖励到账 | 「晒单奖励已到账 17.20」 | 全中并发奖成功后（可紧跟结算通知） |
| 改标题被驳回 | 「改标题未通过」+ 原因 | 运营驳回时 |

点通知进入该方案详情（发布人视角，能看到人数 / 总额 / 奖励）。

### 15.6 命中额外奖励（数字可算）

这是**平台发给晒单人**的奖励，**不是**从跟单用户本金抽成。跟单用户仍按现网串关结算。

| 项 | 口径 |
|---|---|
| 公式 | `奖励 = 该方案跟单总本金 × 奖励比例` |
| 默认比例 | **2%**，后台「晒单奖励比例 %」可配 |
| 发放条件 | **方案全中**（每一腿都打中）后发放 |
| 未中 / 走水 | **不发** |
| 预期奖励（未结算） | `当前累计跟单本金 × 比例`，详情标明「若全中预估，未结算」 |
| 全中后 | 按结算瞬间的跟单总本金计算实发，详情显示「已发放 xxx」 |
| 未达门槛 | 本来就不能发晒单，因此也不会产生奖励 |
| 战绩 | **奖励不写进 AI 作者近 X 场**，也不改晒单用户的注单命中率窗口 |

计算示例：跟单总本金 860，比例 2% → 预期 / 实发 17.20。再有人跟 100 → 未结算预估变为 19.20。

### 15.7 发布人「我的晒单」与详情

- 「我的投注」里已分享且已上广场的单，主按钮改为「查看数据」，整卡可进详情
- 详情在**发布人视角**突出三块：跟单人数、总下注额、预期或已发额外奖励
- 别人（含路人、其他登录用户）点进同一方案，仍是普通跟单详情，**不展示「你的奖励」模块**
- 支持从通知、从我的投注已分享卡一跳进入该详情

### 15.8 风险与原则

- 必须披露「用户分享」，禁止包装成官方或 AI
- 过往命中不代表未来
- 不把用户单或晒单奖励偷偷算进某个 AI 作者战绩
- 不采集其他平台的单
- 跟单风险提示与现网投注负责文案对齐
- 额外奖励由平台账户支出，需财务可对账（方案 id、跟单总本金、比例、实发金额、发放时间）

---

## 16. 原型对照

| 角色 | 入口 | 覆盖 |
|---|---|---|
| 用户 | `prototype/frontend/index.html` | 首页模块、广场、详情（含发布人数据）、作者、晒单用户、跟单半层、我的跟单、我的投注、消息 |
| 运营 | `prototype/backend/index.html` | 概览（待审只计改标题晒单）、作者、策略、方案（免审/改标题标记）、订单、配置（含奖励比例） |

从 `prototype/index.html` 进入即可。演示账号：**阿凯**（近 10 场 8/10，已达门槛）。预置已发布晒单「英超 2串1」（21 人 / 本金 860 / 预估奖励 17.20）。

**点通路径：**

1. 记录 → 我的投注 → 未开赛单「分享到广场」→ 半层无标题框 → 确认 → toast「已发布到广场」→ 广场筛「用户晒单」立刻可见
2. 同一半层点「修改标题」展开，改完再确认 → toast「已提交审核」→ 后台方案筛「用户分享」为待审核
3. 已分享卡「英超 2串1」点「查看数据」：人数、总额、预期奖励
4. 顶栏「讯」或消息列表：例如「有人跟了你的英超 2串1」，点进详情
5. 广场对该晒单再跟一笔 → 回到我的投注 / 详情，人数、总额、预期奖励增加，并多一条通知
6. 后台：免审单已是招募中（「免审·未改标题」）；改标题的才待审
7. 配置可改「晒单奖励比例 %」，详情预估随之变化

切「路人乙 5/10」或看页底「模拟未达标」看禁用态。路人进阿凯的方案看不到「发布人数据 / 你的奖励」。
